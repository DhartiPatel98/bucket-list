const LOCAL_DB_URL = "http://localhost:3000/";

const users = LOCAL_DB_URL + "users";
const companies = LOCAL_DB_URL + "companies";

const wishes = LOCAL_DB_URL + "wishes";
const checklist = LOCAL_DB_URL + "checklist";

module.exports = {
  LOCAL_DB_URL,
  users,
  companies,
  wishes,
  checklist,
};
